apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nordlynx
  namespace: argocd
spec:
  project: default
  source:
    # TODO: update repoURL when pushing to Git
    repoURL: https://github.com/YOUR_ORG/nordlynx
    targetRevision: HEAD
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: nordlynx
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
